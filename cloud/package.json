{
  "name": "turtle-oracle-cloud",
  "version": "1.0.0",
  "private": true,
  "description": "The cloud turtle: the Terrible Turtle Oracle seance as a Cloudflare Worker",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy:staging": "wrangler deploy --env staging",
    "deploy": "wrangler deploy",
    "tail:staging": "wrangler tail --env staging",
    "prepare-assets": "bash prepare-assets.sh",
    "test": "node test/all.mjs",
    "test:seance": "node test/seance.mjs",
    "test:parity": "node test/parity.mjs"
  },
  "devDependencies": {
    "wrangler": "4.125.0"
  }
}
